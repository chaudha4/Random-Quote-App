import {
    StyleSheet
} from 'react-native'

const styles = StyleSheet.create({
    container: {
        flex: 1,
        padding: 24,
        marginTop: 15,
        alignItems: "center",
        backgroundColor: '#0000',
        flexWrap: 'wrap',
        borderColor: "#0001",
        borderWidth: 2,
        borderRadius: 40,
    },
 
      buttonContainer: {
        flexDirection: 'row',
        justifyContent: 'center',
        alignItems: 'center',
        marginTop: 2,
        borderRadius: 2,
    },          
});


export {
    styles
}